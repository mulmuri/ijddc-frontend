import { createTheme } from '@mui/material';

export const footerTheme = createTheme({
  palette: {
    primary: {
      main: "#F7F6F6"
    },
    text: {
      primary: '#000000',
      secondary: '#999999'
    }
  },
});