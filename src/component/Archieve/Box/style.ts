import styled from "@emotion/styled";





export const Box = styled.div({
  width: 300,
  height: 400
})

export const TitleSection = styled.span({

})

export const SummarySection = styled.span({

})