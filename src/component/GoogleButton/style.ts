import styled from "@emotion/styled";
import GoogleLogin from "react-google-login";





export const ResponsiveGoogleLogin = styled(GoogleLogin)({
  width: '100%',
  height: '100%',
  margin: 0,
  padding: 0
})