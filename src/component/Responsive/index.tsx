import styled from "@emotion/styled";
import { Box, Button, IconButton, Paper, TextField } from "@mui/material";





export const ResponsiveButton = styled(Button)({
  width: '100%',
  height: '100%',
  padding: 0
})

export const ResponsiveImg = styled.img({
  width: '100%',
  height: '100%'
})

export const ResponsiveIcon = styled(IconButton)({
  width: "100%",
  height: "100%"
})

export const ResponsiveTextField = styled(TextField)({
  width: '100%',
  height: '100%'
})

export const ResponsivePaper = styled(Paper)({
  width: '100%',
  height: '100%'
})

export const ResponsiveBox = styled(Box)({
  width: '100%',
  height: '100%'
})